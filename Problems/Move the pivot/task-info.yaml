type: code
files:
- name: src/Main.java
  visible: true
  text: |-
    import java.util.*;

    public class Main {

        public static void moveThePivot(int[] array, int pivotIndex) {
            // write your code here
        }

        /* Do not change code below */
        public static void main(String[] args) {
            Scanner scanner = new Scanner(System.in);
            int[] array = Arrays.stream(scanner.nextLine().split(" "))
                    .mapToInt(Integer::parseInt).toArray();
            int pivotIndex = scanner.nextInt();
            moveThePivot(array, pivotIndex);
            Arrays.stream(array).forEach(e -> System.out.print(e + " "));
        }
    }
  learner_created: false
feedback_link: https://hyperskill.org/learn/step/3827
status: Failed
feedback:
  message: |
    Failed test #1 of 7. The pivot is placed on the wrong location!
  time: Mon, 27 Jul 2020 22:11:01 UTC
record: -1
